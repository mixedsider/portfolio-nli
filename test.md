- test : 배포 사이트(https://portfolio.mixedsider.cloud) NLI 입력창에 501자 문자열을 전송
- 예상결과 : Gateway의 입력 길이 검증 실패를 사용자에게 알리고, 입력창과 전송 버튼이 다시 사용 가능해진다.
- 실제결과 : Gateway는 HTTP 413 및 `질문은 500자 이하로 입력해주세요.`를 반환했지만, 프론트엔드는 `도우미 Gateway에 연결할 수 없습니다. Gateway가 켜져 있는지 확인해주세요.`로 표시했다. 입력창과 전송 버튼은 다시 사용 가능해졌다.
- 문제 분석 : `nli-widget.js`에서 non-2xx 응답을 모두 예외로 처리한 뒤 catch 절에서 연결 실패 문구만 렌더링한다. 따라서 Gateway가 제공하는 안전한 공개 오류 메시지(413, 429 등)가 사용자에게 전달되지 않는다.
- 해결 방법 : non-2xx 응답의 JSON `message`를 안전하게 파싱해 표시하고, 4xx 입력/요청 제한 오류와 5xx·네트워크 오류를 구분한다. 필요하면 클라이언트에도 500자 길이 검증을 추가한다.
